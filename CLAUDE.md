# 화이트샌즈 판매일보 웹앱

## 프로젝트 개요
화이트샌즈 매장 판매일보 등록용 단일 HTML 웹앱.

- **파일**: `index.html` 단일 파일
- **GitHub**: https://github.com/ny7742-hub/record
- **배포 URL**: https://ny7742-hub.github.io/record/
- **외부 라이브러리**: SheetJS CDN (`xlsx.full.min.js` v0.18.5)


## 기본 기능
- 상품명 + 칼라 통합 검색 (실시간)
- 칼라 칩 자동 표시 및 선택
- 키보드 방향키로 검색결과 탐색, Enter로 추가
- 선택 목록 최신순 정렬, 중복 추가 허용
- 종류별/전체 할인율 일괄 적용
- 판매 양식 엑셀 다운로드 (시트명: 매장판매양식)
- 상품 데이터 업데이트 기능 (xlsx 업로드 → localStorage + Firebase로 팀 전체 공유, 7일 이상 지나면 헤더에 업데이트 필요 경고 배지 표시)
  — 헤더의 경고 옆 `📤 상품데이터 업로드` 버튼이 업로드 모달(`openUpdateModal`)을 바로 연다(경고가 뜨면 빨간 버튼, 평소엔 흐린 버튼).
- 매장코드 자동변환: 센텀→LSC, 동부산→LDB, 평촌→LPC, 김포→LKP
- **백화점몰(온라인) 매장은 오프라인 매장과 코드가 다르다**(2026-09-18 영업2팀): `(백)롯데본점 LTOA` · `(백)현대천호 HDOC` · `(백)롯데고양 LGY`.
  `STORE_MAP` 맨 앞에 넣어 `본점`이 오프라인 `LDA`로 먼저 걸리지 않게 하고, 키는 `(백)…`·코드만 둔다(`롯데본점`을 키로 넣으면 오프라인 이름과 겹친다).
  오프라인은 그대로 `본점 LDA` · `천호 HCH`.
  **수기발주 매장 목록(`ORDER_STORES`)에도 세 매장을 넣었다** — 수령인·연락처·주소는 아직 받지 못해 비워 두었다(발주서에 나가려면 채워야 한다).

## 상세 문서
기능별 상세는 아래 문서에 있다. **그 기능을 건드릴 때만 해당 문서를 연다** — 전부 읽지 말 것.

- [`docs/costco.md`](docs/costco.md) — 코스트코 발주(PDF 주문서 · DSS Export · 송장), 판매현황 표·세일, 직매입
- [`docs/orders.md`](docs/orders.md) — 사입출고등록, 아난티, 트래블메이트, CX팀 물류센터, 선물세트, 미출고 반영, 송장건수
- [`docs/stock.md`](docs/stock.md) — 창고재고 업로드·공유, 백화점몰·홈쇼핑몰·코스트코 재고확인, 재고이관 분석
- [`docs/sales.md`](docs/sales.md) — 홈 연매출·전체매출, 매장판매일보 업로드, 백화점 탭, 매장별 일별매출, 홈쇼핑 탭
- [`docs/margin.md`](docs/margin.md) — 행사 마진 계산서, 제안 상품 손익계산서, 코스트코·백화점 공문, 행사약정서 PDF
- [`docs/returns.md`](docs/returns.md) — 홈쇼핑·코스트코 반품등록(시트 불러오기 · 코드매칭 · 창고간이동)
- [`docs/auth.md`](docs/auth.md) — 회원 승인제(이메일 링크 로그인 · 승인 · Firebase 규칙)

## 바로가기
- **로그인(SSO) 주소를 직접 조립하지 말 것.** 코스트코 DSS 바로가기가 Keycloak `openid-connect/auth?...`를
  직접 만들고 `redirect_uri`에 `#/costcokr/dropship/`(이미 프래그먼트가 있는 주소)를 넣어 두었는데,
  `response_mode=fragment`라 로그인 후 `#/costcokr/dropship/&session_state=…&code=…`처럼 프래그먼트가 덧붙어
  SPA 라우터가 경로를 못 찾고 **404**가 났다. 바로가기는 서비스 주소만 두고 로그인은 그쪽에 맡긴다.

## 모바일 (≤700px)
- 메인 `</style>` 바로 앞 `모바일 보정` 블록에 모은다 — 앞쪽 규칙이 `!important`라 뒤에 와야 이긴다.
- 백화점 카드: 카드를 화면 폭(`calc(100vw - 58px)`)으로 넓히고 매출 현황(`s-tbl`)·베스트(`b-tbl`)를 `table-layout:fixed`로 줄여
  **합계 열이 잘리지 않게** 한다(예전엔 270px 카드에 364px 표라 잠실·청량리 합계가 안 보였다).
- 바로가기(`#qcBar`)는 **맨 아래로 옮기고**(`_qcPlace`, 자리표시 `#qcBarHome`, resize·DOMContentLoaded에서 다시 판단) **한 줄에 3개** 격자.

## 화면 색
- 배경이 크림색(`#fbf6ec`)이라 옅은 회색 글자는 잘 안 보인다. **힌트·보조 문구는 `#5b6675`**(대비 5.4:1)를 쓴다.
  예전에 쓰던 `#94a3b8`은 대비가 2.4:1이라 읽기 어려워 전부 바꿨다 — 새 문구를 넣을 때도 `#94a3b8`을 쓰지 말 것.
  단 상단 네비·홈 상단 카드처럼 **어두운 배경 위**에서는 밝은 색(`#cfe6e6`·`#a5b4fc` 등)이 맞으므로 건드리지 않는다.

## 배포 방법
```powershell
git add index.html
git commit -m "수정 내용"
git push
```
push하면 GitHub Pages에 자동 반영됨 (1~2분 소요).

## 주의사항
- JS 수정 시 문법 오류에 주의 (오류 시 검색 자체가 안 됨)
- `index.html`이 1.9MB로 크므로 Read 시 offset/limit 분할 필요
- 기능을 고치면 `docs/`의 해당 문서도 같이 고친다 — 이 파일에는 기능 상세를 다시 적지 말 것(매 요청마다 통째로 읽힌다).
