# 화이트샌즈 판매일보 웹앱

## 프로젝트 개요
화이트샌즈 매장 판매일보 등록용 단일 HTML 웹앱.

- **파일**: `index.html` 단일 파일
- **GitHub**: https://github.com/ny7742-hub/record
- **배포 URL**: https://ny7742-hub.github.io/record/
- **외부 라이브러리**: SheetJS CDN (`xlsx.full.min.js` v0.18.5)

## 현재 기능
- 상품명 + 칼라 통합 검색 (실시간)
- 칼라 칩 자동 표시 및 선택
- 키보드 방향키로 검색결과 탐색, Enter로 추가
- 선택 목록 최신순 정렬, 중복 추가 허용
- 종류별/전체 할인율 일괄 적용
- 판매 양식 엑셀 다운로드 (시트명: 매장판매양식)
- 상품 데이터 업데이트 기능 (xlsx 업로드 → localStorage + Firebase로 팀 전체 공유, 7일 이상 지나면 헤더에 업데이트 필요 경고 배지 표시)
- 매장코드 자동변환: 센텀→LSC, 동부산→LDB, 평촌→LPC, 김포→LKP
- 코스트코 발주: PDF 주문서의 코스트코 상품코드(6자리)를 `CC_PDF_CODE_MAP`으로 자사 상품코드에 연결한 뒤,
  상품데이터에서 자사코드·바코드를 찾는다. **신상품이 코스트코에 등록되면 이 표에 코드를 추가해야 매칭된다**
  — 코스트코 판매현황 상품목록(`costcoDB.products`)에만 넣으면 발주 매칭은 계속 미매칭으로 뜬다.
  상품데이터 조회는 업로드본(`RAW`) → 내장 스냅샷(`DEFAULT_DATA`) 순서(`_ccPdfFindProductRow`).
  바르셀(691716 / `WLW44H5S`)은 **코스트코 단독상품**이라 상품명에 시즌 프리픽스가 없고,
  플레이엠디에 TAG가·현재가가 0으로 등록돼 있어 `CC_ONLY_PRICE`로 25,990원을 채운다(값이 0일 때만).
  **장우산 850**(696151 / `GLM104Z9Z`, 블랙 `BKX`·딥그린 `DGEX`)도 같은 경우로 18,990원을 쓴다 —
  코스트코 컬러 `DG`는 자사 `DGE`라 `CC_PDF_COLOR_MAP`에 넣어야 한다.
  발주 금액은 `_ccPdfCostcoAmount`가 판매현황 상품 판매가에서 가져오는데, 단독상품은 `직매입`(매입가) 행이
  잡히므로 **`CC_ONLY_PRICE`가 있으면 그 값을 먼저 쓴다**(장우산이 14,608원으로 나오던 문제).
  코스트코 판매현황의 `바르셀` 정상가도 25,990원으로 강제 보정 — `바르셀 직매입`(20,656원 매입가)은 별개 행이라 그대로 둔다.
  5단계 `⬇ 발송일괄업로드`를 받으면 **"코스트코 탭의 매출을 등록할까요?"** 모달이 뜬다(예/아니오).
  **등록 자료는 5단계 파일이 우선이다**(`_ccPdfSalesRowsFromTarget`) — 1단계 PDF 없이 5단계만으로도 등록된다.
  DSS Export의 `item.retailerSKU`(`696151-BK`)로 `CC_PDF_CODE_MAP`을 찾아 상품·색상을 정하고,
  수량은 `quantity`, 금액은 코스트코 판매가×수량, **판매일은 파일명의 날짜**(`PurchaseOrderDetail_09_03_2026` → 9/3).
  5단계에서 못 찾으면 1단계 PDF(`ccPdfRows`)나 `ccPdfSalesBackup`을 쓴다 — `ccPdfRows`는 메모리 전용이라
  새로고침하면 사라지므로 등록에 필요한 값만 localStorage에 남긴다. **수취인·연락처·주소는 저장하지 않는다.**
  예를 누르면 주문일자를 판매일(M/D)로 삼아 상품·색상별 수량·금액을 코스트코 판매 현황에 **더한다**
  (판매현황 파일 업로드처럼 그 날짜를 통째로 덮어쓰지 않는다).
  2개입 세트상품(`CC_SET_PRODS`)은 업로드 규칙과 동일하게 개수÷2 = 세트 수로 저장한다.
  상품·색상을 못 맞춘 줄은 등록하지 않고 모달에 이유를 적는다.
  **중복 등록 방지는 4중이다** —
  ① 같은 PDF 재업로드: `_ccPdfRowKey`(주문번호|상품코드|컬러코드|원상품명|수량)로 이미 있는 주문 줄은 안 쌓는다.
  ② 같은 주문 재등록: `costcoDB.ccPdfDone`(주문번호|상품|색상#n → 등록시각, 180일 보관)에 남겨 두 번 반영 안 한다.
  Firebase에서 원격 costcoDB로 교체될 때 `discontinued`처럼 **로컬 `ccPdfDone`을 병합**해 이력이 유실되지 않게 한다.
  ③ 판매현황 파일과의 이중계상: 그 날짜에 파일 업로드 실적(`sales[기간]._uploaded`)이 있으면 **기본 제외**하고
  모달에 경고 + 체크박스로만 강제 포함할 수 있다. 그래서 발주 등록은 `_uploaded`를 켜지 않고 `_pdfReg`만 남긴다
  (`_uploaded`는 `ccMergeWeekMap` = 파일 업로드 전용 표시여야 경고 근거가 된다).
  ④ '예' 연타: `_ccPdfSalesBusy` + 계획 즉시 분리.
  **5단계 다운로드는 DSS가 내려준 원본 xlsx를 그대로 두고 J/K(택배사·송장번호) 셀만 고쳐 쓴다**
  (`_ccPdfBuildInvoiceXlsx`, 아난티와 같은 zip 직접 편집). SheetJS `aoa_to_sheet`로 새로 만들면
  송장번호를 못 찾은 행에도 `<c r="K5" t="str"><v></v></c>` 빈 셀이 생겨
  DSS 발송업로드가 `Shipment upload failed due to an unexpected error`로 거부한다.
  셀은 DSS 자신이 쓰는 방식(`t="str"` + `<v>`, 그 행의 `s=` 스타일 재사용)으로 넣는다.
  파일명은 `<원본이름>_송장.xlsx`. 원본이 `.xls`이거나 zip 편집이 실패하면 옛 방식으로 되돌아간다.
  DSS Export(PurchaseOrderDetail) 구조: 1행 표시헤더 · **2행 필드경로**(`shipmentDetails.shipment.trackingNo`) · 3행부터 데이터,
  주문번호는 A열(`customerOrder`), 택배사 J열, 송장번호 K열.
- 코스트코 직매입 행(`바르셀 직매입`·`장우산 850 직매입`)도 상품 페이지 바로가기를 둔다.
  예전에는 이름이 `직매입`으로 끝나면 `url`을 강제로 `""`로 지워, 상품관리에서 넣어도 새로고침하면 사라졌다.
  기본 주소는 `CC_PRODUCT_URLS`에 있고, `url`이 없거나 **빈 문자열일 때** 채운다.
- 코스트코 판매현황 표: **그 달에 판매가 없어도 보여준다**(뭘 못 팔았는지 봐야 하므로).
  단 **코스트코에서 한 번도 팔린 적 없는 이름은 뺀다** — `costcoDB.products`에 다른 채널 상품
  (`라자르`·`로디아`·`미카엘`·`네루아` 등)이 섞여 들어와 있어, 전부 띄우면 코스트코 상품이 아닌 것까지 나온다.
  즉 `_ccIsDefaultProd(기본 등록) || _ccProdHasAnySales(판매 이력)`이고, 세일 등록 상품도 보여준다.
  개별로 숨기고 다시 켜는 것은 **`구분` 헤더의 👁 버튼**에서 한다(`hiddenProds`) —
  `상품 숨김 / 표시` 패널에 전체 상품이 체크박스로 뜨고, 끄면 표에서 빠지고 다시 켜면 돌아온다.
  **숨긴 상품도 패널 목록에는 남는다** — 목록에서까지 빠지면 다시 켤 방법이 없다.
  숨긴 게 있으면 `구분` 헤더에 `숨김 N`을 띄운다(안 보이는 상품이 있다는 걸 알아야 하므로).
  **진열 순서·묶음은 `CC_PROD_GROUPS`(영업2팀 지정)를 따른다** — 바이어 `D26`/`D31`과 `직매입` 세 묶음이고,
  묶음은 **맨 왼쪽 열**(`cc-sticky-0`/`cc-grp-cell`, 폭 54px)에 rowspan으로 묶는다 — 글자는 가로로 쓴다(세로쓰기는 읽기 나빴다).
  **표 선은 팝업 탭처럼 한 가지로 통일**한다 — 모든 칸 `1px solid #b9c4d4`.
  날짜 경계·고정 열 경계에 따로 굵은 선을 두지 않는다(강조는 묶음 경계선 하나만).
  표 마감선(`cc-last-row`)은 다른 칸 선과 같은 두께로 두고,
  묶음이 끝나는 줄(`cc-grp-end`)에는 청록 2px 색선을 그어 D26/D31/직매입을 갈라 보이게 한다.
  고정 열이 넷이 되면서 `ccSyncStickyOffsets`는 **열 폭을 더해 sticky left를 계산**한다 —
  예전의 '스크롤로 밀어붙여 렌더 위치를 재는' 방식은 묶음 열이 생긴 뒤 8~17px 모자라게 수렴해 열이 겹쳤다.
  세일 모달 상품 목록도 같은 순서·묶음으로 보여준다(`renderSaleModal`).
  **끝난 세일은 세일 설정 목록에서 감춘다**(진행중·예정만, 감춘 건수는 안내로 표시) —
  자료는 지우지 않는다. 지난 달 표의 세일가와 판매현황 위 세일 배지(`renderCcSaleSummary`)가 그대로 나와야 하기 때문.
  `CC_PROD_ORDER`는 이 묶음에서 자동으로 만들어지고, 목록에 없는 상품은 `기타`로 맨 뒤에 붙는다
  (새로 생긴 상품이 조용히 사라지지 않게). 순서를 바꾸려면 `CC_PROD_GROUPS`만 고치면 된다.
- 사입출고등록: 홈쇼핑 주문서는 결제금액이 비어 오는 경우가 많아,
  비면 `홈쇼핑 매칭표의 현재가 → 상품데이터(자사코드) 현재가/TAG가` 순으로 단가를 찾아
  `단가 × 수량`으로 채우고 표에 총 수량·총 판매금액을 보여준다(자동 채운 건은 💡 표시, 직접 수정하면 해제).
  주문 상품명 정규화는 **맨 앞의** `(최초공개)`·`(단독)` 같은 괄호 홍보문구를 제거한다
  (이름 중간의 `(Ardes)`는 주문서에서 괄호 없이 오기도 해서 통째로 지우면 오히려 깨진다).
  컬러명은 정확일치 후 **부분일치**로 한 번 더 찾는다 — 주문 `베이지` ↔ 상품데이터 `(핑크&크림)베이지`.
  단 부분일치 후보가 둘 이상이면 비워 둔다.
  컬러를 못 읽었는데 후보 색이 여럿이면 코드를 비워 둔다 — 첫 색을 임의로 고르면 엉뚱한 코드가 나간다.
- 반품등록 탭에는 **홈쇼핑 반품등록**과 **코스트코 반품등록** 두 섹션이 있고, 둘 다 4단계다
  (1 반품리스트 열기 · 2 붙여넣기 · 3 코드매칭 · 4 사입반품양식 다운로드).
  1단계 링크는 같은 시트의 다른 탭을 연다 — 홈쇼핑은 `gid=36451127`, 코스트코는 `gid=0`(온라인).
  코스트코 1단계 안내는 **온라인 탭에서 `처리완료·입고일·처리일·해체일`이 초록색으로 표시된 내역을
  `온라인(완료)` 시트로 옮긴 뒤 2단계에 붙여넣으라**고 적어 둔다(업무 매뉴얼의 '연두색/해체일 기재건').
  홈쇼핑 쪽에는 **5단계 시트 정리** 안내가 더 있다 —
  ① 홈쇼핑 → `온라인(완료)`에 해당 건 복사 후 홈쇼핑에서 삭제 ② 불량건은 창고간 이동.
  코스트코 쪽은 CX팀_반품리스트(구글 시트) **`온라인` 탭**에서 `처리상태 ~ 결제가`(15열)를 그대로 붙여넣는다
  **열 위치는 고정하지 않고 내용으로 찾는다**(`_ccrMapRow`) — 복사 범위가 한 칸만 밀려도
  고정 위치로는 상품명 자리가 어긋나 전부 미매칭이 됐다.
  상품명은 **점수로 고른다** — `화이트샌즈`가 든 칸 중 `[`로 시작하고 바로 뒤가 수량(작은 숫자)인 칸이 이긴다.
  가장 긴 칸을 쓰면 `333165084-1 #694099-BK 화이트샌즈 … 합포장하셨다며 전달 요청` 같은 **비고 칸**을 집어
  상품명이 통째로 어긋난다(`#`·`합포장`·`요청`·주문번호가 든 칸은 감점).
  수량·결제가는 상품명 뒤 첫 숫자칸과 다음 칸,
  상태는 `CCR_STATES` 중 하나가 든 칸, 날짜는 상품명 앞의 날짜칸들(마지막이 해체일)에서 찾는다.
  **`처리보류`·`확인요청`·`출고예정`은 제외**한다(`CCR_SKIP`) — 아직 끝난 건이 아니다.
  상품명 → 상품컬러코드는 `_hsFindMatchFromDefaultData`로 찾고, 공급가 = 결제가 ÷ 수량.
  결제가가 `미매핑`처럼 숫자가 아니면 비워 두고 표에서 직접 채운다.
  **반품일자 = 물류입고일**(매뉴얼 `반품 입력_이미지 매뉴얼` 기준), 매장·창고코드는 4단계 입력칸(기본 `COSO`/`NH`).
  **다운로드 대상·정렬 모두 물류입고일 기준**이다(`_ccrReady`·`_ccrSortByIn`) —
  입고일이 없는 행만 회색으로 두고 뺀다. 예전에는 해체일 기재건만 받았는데,
  반품일자 자체가 입고일이라 해체일이 아직 안 적힌 건도 등록해야 한다(2026-09 영업2팀 지정).
  날짜는 **쇼핑몰 바로 앞 세 칸**(입고일·처리일·해체일)을 자리로 읽는다 — 날짜가 든 칸만 모으면
  해체일이 비었을 때 처리일을 해체일로 잘못 읽는다.
  단계는 7단계 — 5 플레이엠디 등록(`[EX]사입매장출고반품등록`), 6 불량건 창고이동, 7 시트 정리.
  시트 맨 뒤 물류답변이 `TRUE`면 표의 `불량` 칸이 자동으로 켜지고(직접 체크·해제 가능),
  6단계 `📥 창고간이동 다운로드`가 **불량 건만** 창고이동 양식(8열)으로 내려준다
  (반품창고 `NH` → 입고창고 `BAD`, 상품코드 칸에 상품컬러코드, 이동일자 = 물류입고일).
  6단계는 플레이엠디 `[EX]창고입고반품이동등록`에 **작업구분 `(M)창고간이동` · 파일형태 `바코드`**로 올린다.
  수취인·연락처·주소는 읽지도 저장하지도 않는다.
  **홈쇼핑 반품등록도 같은 `_ccrMapRow`를 쓴다**(머리글 없이 붙여넣은 경우) — 예전에는 고정 위치(`prod:11`)로
  읽어서, 반품리스트처럼 앞에 `처리상태` 열이 하나 더 붙으면 주소를 상품명으로 읽어 코드매칭이 전부 실패했다.
  쇼핑몰(채널)은 반품매장코드를 정하는 값이라 `코스트코|홈쇼핑|면세|GS|CJ|SSG…` 키워드로 넓게 잡는다.
- 홈쇼핑 탭: 상품명 옆 `구분` 열에서 진행 상태를 고른다(`HS_STAGES` — 라이브확정·녹화확정·현대제안·롯데제안·GS제안·홈앤제안).
  `확정`으로 끝나면 초록, `제안`으로 끝나면 주황으로 칠해 한눈에 갈린다. 비워 두면 미지정.
- 공문 작성 (`💰 마진계산 · 공문` 탭 아래쪽 — **마진계산과 한 화면**이다, 2026-09 영업2팀): 코스트코(**바이어 D26·D31 둘로 나뉜다**)와 백화점 공문을 한자리에서 만든다.
  위쪽 버튼으로 `코스트코 D26` · `코스트코 D31` · `백화점`을 고르고, 고른 값이 공문 `수신`에 그대로 들어간다.
  **행사약정서(KO COUPON CONTRACT) PDF를 올리면 공문이 저절로 채워진다**(`docLoadContract`) —
  약정서 한 장에 공문에 필요한 값이 다 있다:
  `Dept / Buyer`(26·31) → 수신 · `Item #`(6자리) · `Item Description`(영문 상품명) ·
  `Start Date - End Date` → 기간(`marginDB.period`) · `Coupon Value` · `Vendor Fund` · `Costco Fund` ·
  Inline/Online(금액이 붙은 쪽) → 판매처. 업로드 칸은 공문 카드 맨 위에 있다(`.doc-ct-drop`).
  **마진계산 행을 고르지 않아도 약정서 값으로 공문이 완성된다**(`_mgCtItems`가 `contract.fromContract`로 물러선다) —
  예전에는 마진계산 표에서 행을 골라야만 `3. 내용`이 채워졌다.
  제목은 품목이 하나면 상품코드를 앞에 붙인다(`온라인 상품 696151 WHITE SANDS LONG UMBRELLA 행사 진행건`) —
  한글 공문 원본 표기와 같다.
- 마진계산 → 코스트코 행사 공문 (같은 탭 아래쪽 공문 화면):
  엔에이치그룹 공문 **A4 1장**을 PDF로 만든다(html2canvas + jsPDF, 아난티 명세서와 같은 방식).
  마진계산 표와 공문은 **한 탭**이다 — 표에서 금액을 보고 바로 아래에서 공문을 만든다.
  **금액은 마진계산 표에서 VAT 제외로 자동 산출** — `쿠폰=(판매가−쿠폰적용가)÷1.1`,
  `Vendor fund=당사부담금÷1.1`, `Costco Fund=코스트코부담÷1.1`. 상품코드가 그대로 공문 내용의 품번이 된다.
  **행사기간은 따로 입력받지 않고 맨 위 `행사기간` 칸(`marginDB.period`)을 파싱해서 쓴다**(`_mgCtPeriod`) —
  `6/8~6/21`·`9/14 ~ 9/27`·`2026.09.14~2026.09.27`·`2026년 9월 14일 ~ …` 형태를 읽고,
  연도를 안 적으면 올해로 보며 종료가 시작보다 앞서면(연말~연초) 종료만 다음 해로 넘긴다.
  수신은 `코스트코 상품본부(D31)`·`(D26)` 중 선택, 판매처는 `코스트코 인라인`·`온라인` 중 선택이고
  **판매처가 공문 제목의 `인라인/온라인 상품`을 정한다**. 문서번호는 발송일자 기준 `NH-MMDD-01` 자동(직접 적으면 우선).
  상품이 여러 건이면 행마다 `공문 표기명`을 적을 수 있고, 비우면 제목의 영문 상품명을 쓴다.
  직인은 이미지 업로드 시 자동 날인되며 `localStorage.mgSealImg`에만 둔다(용량·개인도장이라 팀 공유 안 함).
  직인 등록·변경·삭제는 **공문 카드 머리에 늘 보이게** 둔다(`mgRenderSealBar`) — 입력값을 접어 두면 못 찾는다.
  직인은 **이름 오른쪽 옆자리**에 찍는다 — 직인이 있으면 그 폭(74px+여백)만큼 서명란 오른쪽을 비우고
  그 빈자리에 놓는다. 이름 위에 겹쳐 놓으면 `대 표 이 사 나경화`가 가려진다.
  공문 글꼴 목록에 **고정폭(BatangChe)을 넣지 말 것** — 바탕이 없는 PC에서 그걸로 그려지면
  영문·괄호·마침표까지 한글 한 칸 폭을 차지해 `NH -0910 -01`처럼 낱말이 벌어진다.
  공문에 적는 영문 상품명은 **브랜드를 붙여 쓴다**(`_mgCtEnName`: `WHITE SANDS` → `WHITESANDS`) —
  약정서에는 띄어 오지만 한글 공문 원본은 `WhiteSands`로 붙여 쓴다. 검토 요청 문구는 그대로 띄어 쓴다.
  **검토 요청 문구**(`mgBuildNote`)는 2026-09 영업2팀 지정 형식이다 —
  `코스트코 <영문상품명>(<한글상품명>) 상품의 매출 활성화를 위해 …`로 시작하고
  행사기간·판매가·쿠폰금액(VAT+)·원가·수수료·마진 순으로 적으며 **확인 요청이 맨 끝**에 온다.
  영문명은 행사약정서에서 읽은 것을 쓰고, 택배비는 값이 있을 때만, 창고재고 줄은 넣지 않는다.
  **마진 5,000원 이하 경고(※)는 문구에 넣지 않는다** — 그대로 붙여넣어 보내는 글이라 화면에만 띄운다.
  KO COUPON CONTRACT(코스트코 쿠폰 계약서) 페이지는 만들지 않는다 — 그 양식은 코스트코 것을 직접 작성한다.
- 아난티 발주 (`🏨 아난티 발주`): 아난티가 보낸 엑셀을 올리면 **바코드**로 자사 상품을 매칭해
  도매발주서·거래명세서로 바꾼다. **양식이 두 가지다** —
  `위탁 거래명세서`는 바코드를 **비고열**에 넣어 보내고(규격·면과세 열 있음),
  `발주 요청 리스트`는 **바코드 전용 열**이 따로 있다(비고는 빔). 전용 열이 있으면 그쪽을 먼저 본다(`cBar`).
  공급율도 양식마다 달라 `공급율` 값이 없으면 `수수료`를 찾아 `공급율 = 1 − 수수료`로 환산한다(둘 다 없으면 0.7).
  문서번호 칸에 위탁 양식은 문서번호(46139)가, 발주 요청 리스트는 날짜가 들어와서 **숫자일 때만** 다음 번호를 제안한다.
  품목명 접두사(`LE_에이치티와이(화이트샌즈)_…`)는 회사명까지만 떼고, 그 뒤 시즌·행사 표기(`C.O`·`26SS`·`50%`)만 추가로 뗀다 —
  무조건 다음 덩어리를 떼면 `페미닌 브로더리…`가 `브로더리…`로, `50%비즈 숏 스트랩`이 `숏 스트랩`으로 깎인다.
  **5단계 거래명세서는 아난티 코브(ANC)만 '위탁 양식'** 이다 — 1단계에 어떤 발주서를 올렸든
  내장 템플릿(`AN_WT_TPL`, base64)으로 발행한다. 품목행 10~49(40건) + 총합계 50행이고,
  남는 행은 기존 삭제 로직이 잘라내며 총합계·SUM 범위·F8/N8 참조가 따라 이동한다.
  **양식은 2026-09 아난티 제공본**(`아난티 코브_살롱_리오더_화이트샌즈_260908.xlsx`)이다 —
  열은 `일자 B·C / 품목명 D / 바코드 G / 수량 H / 판매가 I / 매입단가 K / 공급가액 L / 세액 N / 합계 O / 비고 Q`이고
  **규격·면과세 열은 없다**(옛 양식에는 있었다). `C3`에는 문서번호가 아니라 **명세서 일자**가 들어가고(`dateDoc`),
  `T1`에는 **수수료(=1−공급율)**를 넣는다(`feeCell`) — 매입단가 수식이 `I*(1-$T$1)/1.1`이라
  T1이 비면 매입단가·공급가액·세액·합계가 전부 `#VALUE!`가 된다.
  그래서 **`_anInvoiceTpl()`이 `dateDoc`·`feeCell`·`feeRow`까지 넘겨야 한다** — 빼먹으면 일자가 안 찍힌다.
  아난티 원본은 품목행이 11줄(10~20)뿐이라 **40줄로 넓혀서** 넣었고, 넣기 전에
  샘플 데이터·캐시된 `#VALUE!`와 `t="e"`/`t="str"`를 지웠다 — **타입이 남아 있으면 매입단가·공급가액 칸이
  숫자 서식 없이 오류로 보인다**(앱은 `<v>`에 계산값만 넣지 타입은 못 고친다).
  셀을 훑을 때는 `<c ...\/>|<c ...>...<\/c>` 두 갈래를 **모두** 잡아야 한다 —
  자체닫힘 셀을 놓치면 뒤 셀들이 앞 셀 안으로 빨려 들어가 `t="e"`가 그대로 남는다(실제로 한 번 당했다).
  공급자(에이치티와이) 정보는 템플릿에 미리 박아 두었다(M3·K4·Q4·K5·K6·Q6).
  **엑셀 명세서와 PDF 명세서는 같은 문서다**(2026-09 영업2팀 지정) — 미리보기를 따로 그리지 않고
  **방금 만든 xlsx를 읽어 그대로 표로 그린다**(`_xlsxToHtml`). 셀 값·병합·열폭·행높이·테두리·정렬·글꼴·
  표시형식(#,##0 · 0%)을 옮기고, PDF는 그 표를 html2canvas로 떠서 만든다.
  덕분에 양식을 바꿔도 엑셀만 고치면 PDF가 따라온다.
  아난티 위탁 양식은 **B~Q열까지가 문서**다 — 오른쪽 `S1`(수수료)·`T3`(안내문구)는 보조 칸이라
  `_xlsxToHtml(bytes,17)`로 잘라낸다.
  **원본의 노란 음영은 템플릿에서 지웠다** — 아난티가 '여기를 채우라'고 칠해 둔 표시일 뿐이라
  우리가 발행하는 엑셀·PDF에는 남기지 않는다(2026-09 영업2팀).
  **줄바꿈(wrapText)이 없는 칸은 옆 빈 칸으로 넘쳐 흐르게 둔다**(`white-space:nowrap;overflow:visible`) —
  엑셀이 그렇게 보여주기 때문이다. HTML 기본값(자동 줄바꿈)으로 두면 품목명이 좁은 D열 안에서 접히며
  글자가 겹쳐 보인다(D10:F10 병합이 없는 양식이라 더 그렇다).
  화면에서는 폭이 넓어 `_anFitPreview()`가 축소해 보여주고, **PDF를 뜰 때는 축소를 잠깐 푼다**(안 그러면 글씨가 뭉갠다).
  스타일 XML을 훑을 때는 `<xf .../>` 같은 **자체닫힘을 먼저** 잡아야 한다 — 순서를 바꾸면 뒤 항목이
  앞 항목에 빨려 들어가 번호가 밀리고(126개가 118개로) 제목 글꼴 같은 서식이 통째로 사라진다.
  **공급자는 늘 우리(에이치티와이)다**(`_anInvoiceParty`) — 1단계 발주서에서 읽은 `anMeta`에는 buyer만 있어
  그대로 쓰면 공급자 칸이 통째로 빈다.
  매입단가·공급가액·세액·합계·총합계·일금(NUMBERSTRING)은 수식을 그대로 두고 **계산값을 캐시로 함께 기록**한다
  (`_rowSetFormulaVal`) — 엑셀이 재계산하기 전에도 숫자가 보이도록.
  **마지막 품목행의 굵은 아랫선(`thickBot`)은 행을 잘라낸 뒤 새 마지막 행으로 옮긴다** —
  안 옮기면 잘린 자리(템플릿의 원래 마지막 행)에 선이 남아 표 중간에 줄이 그이고 실제 마지막 줄엔 아랫선이 없어진다.
  ANC 외 매장은 업로드본을 템플릿으로 쓰되, 셀 열은 **파싱한 헤더 위치**(`anMeta.cols`)로 찾는다 —
  예전엔 위탁 양식 열(D/G/H/I/L/S)이 하드코딩돼 있어 발주 요청 리스트를 올리면 값이 엉뚱한 칸에 들어갔다.
  명세서 파일명의 매장 표기는 `AN_INVOICE_NAME`(ANC → `아난티 코브_살롱`).
  **1~3단계를 건너뛰고 4단계부터 시작할 수 있다** — 도매발주서(27열)만 올리면
  `_anRowsFromWholesale`가 상품컬러코드로 상품데이터를 찾아 표를 만들고, 매장(주문번호(쇼핑몰) 열)·
  명세서 일자·공급율(0.7)까지 채운다. 이때 품목명은 아난티 표기가 아니라 **상품데이터 정식 상품명**이 되고,
  명세서 거래처는 `AN_WT_PARTY`(아난티 코브 위탁 거래처) 기본값을 쓴다(`_anInvoiceParty`).
  품목명은 행사·매입 표기(`C.O`·`50%`)와 끝의 사이즈 코드(`_OS`·`_FR`)만 떼고 시즌(`26SS`)·색상(`_WH`·`_BE`)은 남긴다.
  3단계 도매발주서는 **공용 `_wholesaleWorkbook`** 을 쓴다 — `발주서` + `매장주소록` 두 시트에
  파일명은 `화이트샌즈(MMDD) - 도매발주서.xlsx`(붙여 씀). R열 상품코드는 다른 탭과 같이 **8자로 자른다**
  (`GKM116Z9U`처럼 상품코드가 9자인 상품에서만 갈린다).
  상품명은 업로드한 상품데이터(`DB`)가 내장 스냅샷(`DEFAULT_DATA`)보다 우선이다 —
  스냅샷에 `클래스볼`처럼 옛 표기가 남아 있어도 실제 출력은 최신 상품데이터(`글래스볼`)를 따른다. 매장은 상호·파일명 키워드로 추정(코브→ANC, 코드/가평→ANG, 남해→ANT, 빌라쥬→ANV).
  **위탁 발주서는 판매가 열이 비어 오는 경우가 많다** — 그대로 두면 전액 0원짜리 명세서가 나가므로,
  비었거나 0이면 매칭 상품의 **TAG가(없으면 현재가)**로 채우고 💡로 표시한다(`priceAuto`).
  파일에 판매가가 적혀 있으면 그 값을 그대로 쓴다. 매입단가 = 판매가 × 공급율 ÷ 1.1이고 공급율 기본값은 0.7.
- 창고재고 (발주 공용 · `whStock`): PlayMD `출고관리 > 현황 > 창고재고현황` 엑셀을 한 번 올리면
  **발주 화면 여섯 곳**(발주서 변환 · 수기발주 · 코스트코 발주 · 아난티 발주 · CX팀 물류센터 · 사입출고등록)의
  표에 `창고재고` 열이 뜨고 발주 가능 여부가 보인다. 업로드 칸은 `.wh-slot` 자리에 `whInitSlots()`가 채워 넣고,
  어디서 올리든 같은 자료를 쓴다(끌어오기는 문서 한 곳에서 위임 처리).
  **기본 기준은 주희물류(NH·NH3)**다 — 전체를 더하면 불량창고·생산팀·타팀·샘플 창고까지 잡혀
  실제 출고 가능량보다 훨씬 많아 보인다(전체 3,472품목 vs 주희물류 2,667품목).
  화면에서 `전체 창고 기준`으로 바꿀 수 있고 선택은 브라우저에 남는다.
  창고 파일은 `상품코드+칼라코드` 단위라 자사코드에서 **사이즈 한 글자를 떼고** 맞춘다(재고확인 탭과 같은 규칙).
  **창고재고 10개 이하는 발주 불가**다(`WH_MIN_ORDER`, 2026-09 영업2팀 지정 — 10개는 발주 안 함) — `N (발주불가)`로 빨갛게 적는다.
  11개 이상인데 주문 수량보다 적으면 `부족 N`, 주문 수량의 2배 미만이면 주황으로 표시하고,
  창고재고현황에 없는 코드는 `?`로 둔다.
  파일이 커서(약 100KB) **팀 공유는 하지 않고** 그 브라우저에만 저장한다.
- CX팀 물류센터 발주 — **도매발주서(27열) 파일로도 채운다**(`cxLoadFile`).
  **시트가 여러 장이면 모두 읽는다**(`DP용`·`초도물량`처럼 목적이 달라 나뉘어 온다). `매장주소록`은 건너뛴다.
  본사이동 발주서는 칸을 다르게 쓴다 — `주문번호(사방넷)`=목적, `주문번호(쇼핑몰)`=전산 처리 방법,
  `쇼핑몰`=주소, `택배사`=배송수단. 그래서 **주소는 `주소` 칸을 먼저 보고 비었으면 `쇼핑몰` 칸**을 쓴다.
  표에 `목적·전산 처리 방법·수령인` 열을 두고 **다운로드(D·E·G열)에 그대로 내보낸다** —
  예전에는 이 세 칸을 늘 공백으로 냈다(요청자·비고만 CX팀이 직접 기재).
  배송수단이 파일에 한 가지로만 적혀 있으면 공통값 칸에 올려 둔다.
- 미출고 반영 (`🧾 등록` → `📮 미출고 반영`): 3단계 —
  1 도매발주서 업로드 · 2 미출고 내역 업로드 · 3 미출고 표시 발주서 다운로드.
  물류에서 **운송장번호가 채워져** 돌아온 도매발주서(27열)와 날짜별 시트로 오는 `미출고` 엑셀을 대조해
  **어느 줄이 몇 개 미출고인지** 표시한다.
  미출고 파일은 한 시트에 `차수·운송장번호·미출고 품목·미출고수량·출고수량` **다섯 칸 블록이 가로로 여러 벌**
  놓여 있다(빈 칸 하나를 두고 **6칸 간격**). 첫 블록의 `차수` 자리에는 매장명이 들어온다.
  시트 이름이 `YYYYMMDD`인 것만 날짜 시트로 보고 가장 최근 것을 먼저 고른다(화면에서 바꿀 수 있음).
  **맞추는 열쇠는 운송장번호 + 품목 이름**이다 — 미출고 품목은 `루노아 딥그린`처럼 줄임말이라
  낱말이 발주서 상품명에 **모두** 들어가면 같은 상품으로 본다(공백 제거·소문자 비교).
  같은 운송장·같은 상품이 여러 줄이면 **앞줄부터 수량을 채워 나눈다**.
  못 맞춘 미출고는 버리지 않고 화면 위 경고와 엑셀 `미매칭 미출고` 시트에 남긴다 —
  운송장번호가 그 발주서에 없는(다른 차수·온라인) 건이 섞여 온다.
  다운로드는 **매장출고양식**(2026-09 영업2팀 제공) 6열이다 —
  `출고창고코드(기본 NH·화면입력) · 매장코드(발주서 주문번호(쇼핑몰) 열) · 출고일자(8자리·화면입력) ·
  바코드 · 출고수량 · 비고`, 시트명 `매장출고`, 파일명 `화이트샌즈(MMDD) - 매장출고양식.xlsx`.
  **출고수량은 실출고(발주 − 미출고)**이고 **전량 미출고인 줄은 뺀다**(내보낼 게 없다).
  도매발주서의 바코드 칸은 비어 오므로 **자사코드로 상품데이터에서 찾아 표에 미리 채운다**(`_usBarcode`).
  단종 상품·부자재(쇼핑백 등)는 못 찾으니 **표의 바코드 칸에서 직접 입력**할 수 있고(`usEditBar`,
  못 찾은 칸은 노랗게 `직접 입력`), 요약에 `바코드 없음 N줄`을 띄운다. 다운로드는 표의 값을 그대로 쓴다.
  표의 매장 칸에는 **매장명 + 매장코드**를 같이 적는다(`롯데백화점 청량리점 (AVE)`) — 양식에 나가는 값이 매장코드라서.
- 트래블메이트 발주 (`📦 발주` → `🧳 트래블메이트 발주`): 코스트코 발주처럼 4단계다 —
  1 발주서 업로드 · 2 상품 매칭 확인 · 3 도매발주서 다운로드 · 4 거래명세표.
  트래블메이트가 보낸 `[트래블메이트] 발 주 서` 엑셀은 **시트 하나가 매장 하나**다
  (인천공항1청사점 `TMI1` · 인천공항2청사점 `TMI2` · 김포공항점 `TMG` — `_tmStoreOfSheet`).
  머리글은 `발주수량`이 있는 줄로 찾고 **발주수량이 적힌 줄만** 가져온다.
  **합계 줄에서 멈춘다** — 그 아래 `트래블메이트 매장 정보`의 주소·연락처가 상품명·발주수량 자리에 걸려
  엉뚱한 행(수량 1,065,003,014)이 만들어졌다.
  상품명은 그쪽 표기(`[화이트샌즈] 루노아 우양산 - 베이지`)를 버리고 **상품데이터의 정식 상품명**으로 바꾼다
  (`_tmMatchProduct`) — 도매발주서·명세표가 사내 양식이어야 하기 때문.
  낱말이 **모두** 든 상품만 후보로 보고 색은 정확 → 핵심(`_tmColorCore`: 괄호·공백 제거) → 부분 순으로 좁히며,
  **후보가 둘 이상이면 비워 둔다**(엉뚱한 코드가 발주서에 나가면 안 된다). 못 찾은 줄은 빨갛게 두고 표에서 채운다.
  3단계는 다른 발주와 같은 **27열 도매발주서**(`_wholesaleWorkbook`), 파일명은 `… - 트래블메이트 도매발주서.xlsx`.
  4단계 명세표는 **이 탭의 표가 우선**이고(`_tmInvoiceSource`) 비어 있으면 수기발주에서 모은다.
  자료(`tmOrdRows`)는 한 번 쓰고 마는 것이라 팀 공유 없이 그 브라우저에만 둔다.
- 트래블메이트 거래명세표 (`📦 발주` → `📄 트래블메이트 명세표`):
  트래블메이트가 쓰는 PlayMD 출고내역 양식을 그대로 만든다(2026-04 인천2청사점 명세표 기준).
  발주 표에서 **매장이 트래블메이트(`TMG`·`TMI1`·`TMI2`)인 행만** 모아 매장별로 한 장씩, PDF로 저장한다.
  **매장마다 다른 것은 공급받는자 상호 하나뿐**이라 `TM_INV_NAME`으로 상호만 갈아 끼운다
  (제1여객터미널점 / 제2여객터미널점 / 김포공항점 — 발주 주소록의 매장명과 표기가 다르니 이 표를 고칠 것).
  공급자(엔에이치그룹 634-87-01986)는 고정이고 직인은 마진계산에 올려 둔 도장(`mgSealImg`)을 쓴다.
  상품코드·칼라·사이즈·TAG가는 **자사코드로 상품데이터에서** 찾는다(`_tmSkuInfo`).
  출고 단가는 **TAG가별 고정표가 먼저**다(`TM_UNIT_BY_TAG` — 2026-09 지정: TAG 45,000 → 21,600).
  표에 없는 TAG가만 공급율(기본 0.48)로 계산하고 **어떤 TAG가가 그렇게 됐는지 화면에 적어 준다** —
  새 가격대가 나오면 영업2팀에 물어 고정표에 한 줄 넣는다.
  **공급가액은 단가를 1.1로 나눠 원 단위로 버린 뒤 수량을 곱한다**(`_tmSupply`), 부가세는 금액에서 뺀 나머지 —
  PlayMD 명세표 두 장이 모두 이 규칙과 맞는다(단가 21,600 → 19,636×10 = 196,360 / 단가 45,000 → 40,909×5 = 204,545).
  금액에서 한 번에 나누거나 10원 단위로 버리면 둘 중 한 장이 어긋난다.
  **상품명 칸에는 색상을 붙이지 않는다** — 칼라 칸이 따로 있다(PlayMD 양식).
  **한 장에 품목 22줄**이고 모자란 만큼 빈 줄로 채워 소계가 아래쪽에 오게 한다(`TM_SHEET_ROWS`).
  **전표번호는 매장(장)마다 하나씩 올라간다**(`_tmSlipNo` — PlayMD 원본이 0007·0008로 이어졌다).
  명세표 모달은 수기발주 페이지 안에 적혀 있어 **처음 열 때 body로 옮긴다**(`openTmInvoice`) —
  안 옮기면 트래블메이트 탭에서 열었을 때 그 페이지가 `display:none`이라 내용 크기가 0이 되어
  html2canvas가 빈 캔버스를 만들고 **PDF가 저장되지 않는다**.
- 홈 연매출 (`🛍️ 홈` 상단): `누적`/`월별`/`주간` 토글로 본다(`homeYtdView`, localStorage).
  `주간`은 **월요일~일요일** 기준으로, `◀ 2026년 8월 ▶`로 **달을 옮겨 가며** 그 달에 걸치는 주를 본다
  (`_weeksOfMonth`·`homeWeekYm`, 최신 주가 위). 전주대비를 위해 **한 주 앞을 더 계산해 두고 화면에는 안 그린다**.
  보고 있는 달은 localStorage에 저장하지 않는다 — 저장하면 다음 달에 들어와도 옛 달이 열려 헷갈린다.
  달 초(1~7일)에는 지난 주가 저번 달에 걸쳐 있어 **저번 달을 먼저 펴서** 보여준다.
  주간은 **일자 자료로만** 더한다(`_chDayMap`: 업로드 일자 > 탭 일별 실적) — 월 단위 값은 쪼갤 수 없어서,
  홈쇼핑은 월별 표(확정 실적표 `HS_ACTUAL` 우선)와 금액이 다를 수 있고 그 사실을 표 아래에 적어 둔다.
  일자 자료가 아예 없고 월 값만 있는 채널은 `monthOnly`로 잡아 ⚠로 알린다.
  진행중인 주는 오늘까지만 더하고 전주대비를 비우며, 며칠치인지(`진행중 N일`)를 함께 적는다.
  `누적`은 채널별(백화점·코스트코·홈쇼핑) 올해 1~현재월 vs 전년 동기,
  `월별`은 **채널마다 `금년·전년·전년대비` 3열**을 두고 1~12월 전부 + 맨 아래 `누적매출`(1~현재월 동기 비교) 행이 붙는다.
  22열이라 **가로 스크롤 없이 화면 폭에 맞춘다**(`.ytd-mtbl`) — `table-layout:fixed` + colgroup 퍼센트로 열 폭을 고정한다.
  **금액은 전부 원 단위**로 적는다(2026-09 요구사항 — 천원 단위로 줄이지 말 것).
  13자리 금액이 22열에 들어가야 해서 글자 크기를 고정하지 않고, `_ytdMonthlyScale`이 **실제로 잘리는지 재서**
  (`_ytdHasClipped`) 13→7.5px까지 한 단계씩 내린다(`_ytdApplyFontSize`).
  창 폭 기준 미디어쿼리로 하면 사이드바·확대배율 때문에 실제 표 폭과 어긋나 글자가 잘린다.
  CSS가 `!important`라 인라인도 important로 넣어야 먹고, 나중에 부른 선택자가 앞을 덮는 순서로 적용한다.
  **홈 탭은 팝업·전략회의처럼 전체 폭을 쓴다**(`#page-home>.main`이 `max-width:none`) — 예전 1280px 제한 때는 표 양옆이 비었다.
  하한 7.5px로도 안 들어가는 1240px 미만은 `min-width:1240px`으로 가로 스크롤한다
  (`.ytd-tbl.ytd-mtbl`로 써야 뒤에 오는 `.ytd-tbl{min-width:520px}`에 지지 않는다).
  누적(4열) 표는 `.ytd-stbl{max-width:1000px}`으로 넓은 화면에서 과하게 늘어나지 않게 한다.
  전년대비는 칸이 좁아 소수점 없이 정수%로 적고(`_ytdRateCellS`, 정확한 값은 툴팁),
  세 자리(+798%)는 `.ytd-r3`로 더 작게 — 이 규칙은 `tr.ytd-total td`보다 우선순위가 높아야 누적 행에서도 먹는다.
  현재월 이후는 금년 칸을 비우고 전년만 보여준다.
  집계는 `_homeYtdByMonth` 한 곳에서만 하고 `_homeYtdThisYear`가 그걸 합산한다 — 두 표가 어긋나지 않게.
  **업로드 값이 항상 우선이고, 일자 단위로 섞는다**(`_chMonthVal`) — 6개 채널(`UPLOAD_CHANNELS`) 전부
  홈 연매출 카드 아래 `▾ 매출 업로드`에서 올리거나 직접 적을 수 있다.
  저장 키는 `YYYY-MM`(월) 또는 `YYYY-MM-DD`(일) 둘 다 쓴다(`_extKey`).
  **날짜는 파일에 저장된 원본 값을 그대로 쓴다** — `XLSX.read`를 `cellDates:false`로 읽어
  엑셀의 날짜 정수(2026-09-01 → 46266)를 받고 `_xlSerialYmd`로 변환한다.
  `cellDates:true`로 Date 변환을 거치면 자정 52초 전(`2026-08-31 23:59:08`)으로 나와
  판매일자가 하루씩 밀린다. 숫자 8자리(20260901)·6자리(202608)도 그 자체로 해석한다.
  우선순위는
  **① 월 단위 업로드가 있으면 그 달 통째로 → ② 일자 업로드가 있으면 그 날은 업로드값 + 나머지 날은 탭 실적
  → ③ 아무것도 없으면 탭 실적**이다. 일별 탭 자료는 백화점(`dailySalesDB`)·코스트코(`costcoDB.sales`)만 있다.
  **홈쇼핑만 예외로 확정 실적표가 업로드보다 우선이다**(`TAB_FIRST`) — 방송 후 반품이 몇 달에 걸쳐 잡혀
  판매일보 일자합계와 월 확정치가 어긋나기 때문(2026년 5월 +5,700만 / 6월 −5,500만).
  그 달 `HS_ACTUAL`(올해)·`PREV_YEAR_SALES.hs`(전년)이 있으면 그 값을 쓰고, 없는 달만 업로드/탭으로 내려간다.
  아난티·팩토리·트래블메이트(`EXT_CHANNELS`)는 대응하는 탭이 없어 업로드/직접입력이 유일한 입력 경로다.
  입력표의 회색 숫자는 '지금 홈 표에 반영되는 값'이고, 일자로 올린 달에는 `N일 ✕` 뱃지가 붙어
  건수를 보여주고 누르면 그 달의 일자 업로드만 지운다(`extClearDays`).
  저장은 `extSalesDB = {채널:{"YYYY-MM":금액}}`(팀 공유 `ws/extSales`).
  **날짜 밀림 잔재 정리**(`🧹 중복 정리`) — 옛 업로드가 판매일자를 하루 앞당겨 저장했고(`cellDates:true` 시절),
  고친 뒤 다시 올리면 올바른 날짜로 '추가'되어 두 벌이 남는다. 앞 날짜 금액이 바로 다음 날과 같으면
  앞쪽이 옛 값이므로(`_extFindShifted`) **파일의 판매일자인 뒷 날짜만 남기고** 지운다.
  팀 공유 데이터라 지울 목록을 `#extDupBox`에 먼저 펼쳐 보이고 확인받은 뒤 실행한다(`extShowDupPreview`→`extCleanShifted`).
  업로드 파서(`_extParseRows`)는 헤더에서 **날짜열을 먼저** 잡고, 금액열은 **우선순위 목록**
  (**`판매금액`(AE) 먼저** → `실판매금액` → `매출액` → …)으로 찾는다 — 영업2팀 기준은 판매금액이고,
  실판매금액(AF)은 에누리·마일리지·상품권을 뺀 값이라 집계 기준이 다르다 — PlayMD 판매일보에는 `매출구분`(카드/현금)이 있어
  막연히 '매출'이 든 헤더를 잡으면 값이 0이 되어 **전 행이 버려진다**.
  채널은 `매장구분` 열(백화점·홈쇼핑·코스트코·아난티·트래블메이트·팩토리)로 **자동 분류**한다 —
  화면에 채널 선택 칸은 없다. 그 열이 없는 파일이면 파서가 `needCh`를 돌려주고 올릴 때 한 번만 묻는다. 반품은 실판매금액이 음수라 그대로 더하면 상계된다.
  8만 줄짜리 판매일보는 `XLSX.read`가 25초쯤 걸려 화면이 멈추므로 안내문구를 먼저 그린 뒤
  `setTimeout`으로 넘겨 실행하고, `cellText/cellNF/cellStyles/cellFormula:false`로 읽어 시간을 줄인다. 일자별 파일이면 월 합계로 모으고,
  `채널` 열이 있으면 채널별로 분류(부분일치라 파일에 `신세계팩토리`로 적혀 있어도 `팩토리`로 들어감), 없으면 화면에서 고른 채널로 넣는다.
  **전년 열도 업로드가 우선이다**(`_homePrevMonth`) — 전년도 날짜로 올린 값이 있으면 그 값을 쓰고,
  없으면 내장 전년 실적표(`PREV_YEAR_SALES`)를 쓴다. 전년은 내장표가 월 단위뿐이라 일자 혼합은 하지 않고
  **그 달 업로드 합계가 통째로 대체**하므로, 전년도는 한 달을 통으로 올려야 정확하다.
  아난티·팩토리·트래블메이트는 내장표에 없으므로 전년도분을 올려야 전년대비가 나온다(없으면 `–`).
  전년엔 실적이 있는데 올해가 0인 달은 ⚠로 표시(실제 0이 아니라 미입력일 수 있음).
- 홈 백화점 매장별 (`🛍️ 홈` 연매출 표 아래): **잠실점·청량리점만** 따로 본다(2026-09 영업2팀 지정 — 늘리려면 `HOME_STORES`에 한 줄).
  위 채널 표에서 고른 보기(누적/월별/주간)를 그대로 따라간다(`_homeStoreAppend`).
  금액은 채널 표와 같은 **EDI 합계**(`dailySalesDB.data[ym][day][매장].edi`)를 쓰고,
  매장명 표기가 달마다 달라(`롯데 잠실`↔`롯데 잠실점`) **지점 이름으로 찾는다**(`/잠실/`).
  주간은 채널 주간 표와 같은 달·같은 주(월~일)를 쓰고 오늘까지만 더한다.
  주간 표는 **매장마다 `금액·전주비·전년비` 세 칸**이고 맨 오른쪽에 합계 세 칸이 붙는다.
  **한 달치만 보여주므로 제목에 보고 있는 달을 적고**(`2026년 9월`) 맨 아래에 `N월 합계` 줄을 둔다 —
  합계는 **주를 더한 값이 아니라 그 달 날짜만 다시 더한 값**이다(8/31~9/6 주처럼 주가 달을 걸치기 때문에
  주를 더하면 9월 합계가 부풀려진다). 그 줄의 전년비는 전년 같은 달과 견준다.
  **전년비는 364일(52주) 앞 같은 요일 주**와 견준다 — 날짜로 맞추면 요일이 어긋나 주간 비교가 깨진다
  (2026-08-31 주 ↔ 2025-09-01 주). 그 주 자료가 없으면 `–`, 진행중인 주는 비교를 비운다.
  **전년 자료가 없는 달이 섞이면 누적 전년대비가 부풀려진다** — 매장별 자료는 2025-06부터라
  1~5월이 0으로 잡혀 +135%처럼 보인다. 그 달들을 ⚠로 적어 둔다.
- 매장별 일별매출 `🟡 온라인 주력주차`: 기간 + **대상 매장**을 고르면 그 매장의 `온라인`·`수량` 칸이
  노란색(`#fef3c7`)으로 칠해진다(`_dsIsFocus`). 매장별로 돌아가며 진행하므로 기간마다 매장을 다르게 고른다.
  저장은 `dailySalesDB.onlineFocus=[{start,end,stores:[]}]`(팀 공유 `ws/dailySales`).
  버튼을 누르면 **기간·매장 입력이 맨 위에 뜨고 달력이 바로 열린다**(`showPicker`), 등록 목록은 그 아래.
  시작일을 고르면 **그 주 일요일**까지 종료일이 자동으로 채워진다(월~일 한 주).
  버튼 옆에는 **보고 있는 달에 걸치는 주차를 `매장명 M/D~M/D` 배지**로 띄운다(`renderDsFocusBadges`).
  **끝난 주차도 감추지 않는다** — 목록·배지 모두 진행중/예정/종료를 색으로만 구분한다(`_dsFocusTone`).
- 매장별 일별매출 표 글자: 표 기본 15px · 매출 입력칸 14px · 필드 머리글 13px(2026-09 요구사항 — 더 줄이지 말 것).
  `백화점·수량·온라인·수량·EDI·수량·PLAY MD` 일곱 열은 **머리글·총매출·입력칸 모두 가운데 정렬**이고,
  `날짜`·`달성금액`만 예전 정렬을 쓴다. 칸 폭은 금액 88px·수량 54px가 최소이고,
  **글자가 칸을 벗어나면 안 된다** — 표가 `white-space:nowrap` + 자동 열폭이라 머리글·총매출은 열을 넓히지만
  입력칸(`width:100%`)만은 조용히 잘리므로, 글자를 키울 때는 `input.scrollWidth>clientWidth`로 확인한다
  (현재 86px 폭에 `123,456,700`·`-12,345,600`까지 들어간다).
- 매장별 일별매출: **팝업 기간 밖의 날짜는 칸을 회색**(`#e6eaf0`)으로 칠한다(`_dsIsClosed`) —
  시작 전·종료 후는 물론 **팝업과 팝업 사이 빈 날도** 회색이다(9/18 하루짜리 팝업이면 그 하루만 흰색).
  **기간은 팝업 탭(`popupDB`)이 정본**이다(2026-09 영업2팀 지정) — 백화점 탭 운영기간은 갱신이 늦어 실제와 어긋난다
  (현대 목동: 백화점 탭 `~8/20`, 팝업 탭 `~9/3`). **팝업 탭에 없는 매장은 칠하지 않는다**(퇴점 정규매장 포함).
  한 매장의 팝업 줄이 여러 개면 그 기간들을 모두 흰색으로 둔다(`_dsPopSpans`).
  시작일이 비면 그 전부터, 종료일이 비면 계속 진행중으로 본다.
  매장명 표기가 달라(`현대 목동점`↔`현대 목동`, `롯데 피트인 산본`↔`롯데 산본`, `현대백 본사`↔`현대 본사`)
  붙임말(점·백화점·피트인·몰·팝업)을 떼고 **백화점 브랜드 + 지점명**으로 맞춘다(`_dsNormPop`·`_dsPlaceOf`·`_dsPopRows`).
  이름이 그대로 맞으면 그것만 쓰고, 지점명으로 찾을 때 **후보 매장이 둘 이상이면 포기**한다 —
  엉뚱한 매장이 통째로 회색이 되면 안 되기 때문. 칸 배경 우선순위는 **빨강(EDI 불일치) > 회색(종료) > 노랑(주력주차)**.
  회색이어도 입력은 막지 않는다(종료 뒤 정산·반품이 들어올 수 있다).
- 백화점 탭 매장 기간: 팝업·정규매장 모두 운영기간을 적을 수 있고(`deptStoresMeta[매장].popupPeriod`),
  종료일이 지나면 팝업은 `🏁 팝업종료`, 정규매장은 `🚪 퇴점`으로 표시된다.
  기간 파서는 `2026년3월1일`·`2026-03-01` 같은 연도 표기를 지원하고,
  연도를 안 적었는데 종료가 시작보다 앞서면(연말~연초) 종료만 다음 해로 넘긴다.
- 재고확인 (`📦 재고확인` 드롭다운) — `🏬 백화점몰 재고확인`·`🏭 홈쇼핑몰 재고확인` 두 탭을 묶어 둔다.
- 홈쇼핑몰 재고확인: 결과를 **채널 → 상품**으로 묶고, 상품마다 **카드**로 만들어
  `grid-template-columns:repeat(auto-fill,minmax(320px,1fr))`로 한 줄에 여러 개를 깐다
  (1,400px 폭에서 4열). 표로 늘어놓으면 좌우가 넓어 읽기 나빴다.
  카드 머리에 상품명·색상 수·품절 수, 안쪽에 `칼라 · 재고 · 품절` 줄. 자사코드는 줄 툴팁으로만 둔다.
  채널 안에서는 **품절 많은 상품 → 재고 적은 상품** 순으로 정렬한다.
  **창고재고가 `품절 기준`(기본 30개)보다 적으면 품절로 본다**(`hsOutMin`, 화면에서 조정·localStorage 저장) —
  몇 개 안 남은 컬러는 몰에 걸어둬도 곧 결품이라 품절로 취급한다.
  맨 위에 **할 일 두 목록**을 띄운다 — 상품리스트의 `품절표시`(note)는 몰에 지금 걸린 상태,
  `qty`는 창고재고다. `✅ 품절 해제 가능`(몰 품절 + 창고재고 기준 이상)과
  `⛔ 품절 처리 필요`(몰 판매중 + 창고재고 기준 미만).
  — 해야 할 일이 필터에 가려지면 안 되므로.
- 백화점몰 재고확인: 롯데 파트너오피스 **단품 재고관리 → ExcelExport**
  (`온라인 재고`·`스타일코드`·`옵션` 열)를 올리면 채널상품별 백화점몰 재고를 본다.
  스타일코드는 채널 접미사가 붙어 오므로(`WCFM100L_LT` → `WCFM100L`) `_` 앞만 쓰고,
  옵션 `그레이/free`에서 색상만 떼어 상품데이터와 맞춰 자사코드·바코드를 붙인다
  칼라명 앞 괄호는 색 계열 수식어라 색 이름이 아니다 — `(핑크&크림)베이지`=베이지, `(인디)핑크&베리`=핑크&베리,
  `(X)내츄럴/그레이`=내츄럴/그레이. 괄호를 떼고 `/ & ·`를 지운 **핵심 색명**으로 비교하면
  온라인 표기와 그대로 맞고(`내츄럴그레이`↔`(X)내츄럴/그레이`, `핑크`↔`(인디)핑크&베리`),
  `핑크`가 `(핑크&크림)베이지`에 걸리던 모호함도 없어진다(`_onsColorCore`).
  원문 정확일치 → 핵심 정확일치 → 핵심 부분일치 순이고, 어느 단계든 후보가 둘 이상이면 비운다.
  탭에 들어갈 때 자사코드가 빈 행만 다시 맞춰 본다(`_onsRefreshMatch`) — 파일을 다시 안 올려도 채워지게.
  **보충가능** = 백화점몰 `단품판매상태`가 품절(상태 열이 없으면 재고 0) **이고** 창고재고 > 0.
  바로 채워 넣으면 되는 건이라 요약·필터·정렬 모두 이걸 우선으로 본다.
  두 파일은 **어느 쪽을 먼저 올려도 된다** — 창고재고는 `onsWhPending`(localStorage 전용, 팀 공유 안 함)에
  보관해 뒀다가 백화점몰 파일이 오는 순간 `_onsApplyWh()`로 붙는다. 대조 결과(`rows[].wh`)는 팀에 공유된다.
  화면은 **한 표에 담되 같은 상품명은 `rowspan`으로 셀을 합쳐** 한 덩어리로 보인다(`_onsGroups`).
  열은 `상품명 · 컬러 · 상태 · 창고재고 · 보충여부` 다섯 개뿐 —
  상품코드·자사코드·바코드·현재가·백화점몰 재고수량은 화면에서 빼고 엑셀에만 남긴다(발주에 필요).
  백화점몰 재고수량은 상태 셀 툴팁으로만 보여준다. 표는 `table-layout:fixed`로 열 폭을 고정한다.
  상품 정렬은 보충가능 많은 순 → 품절 많은 순 → 재고 적은 순.
  결과는 팀 공유(`onlineStockDB` / `ws/onlineStock`).
- 재고이관 분석 (`🔄 재고이관` 탭): PlayMD **매장재고현황 + 창고재고현황 + 매장판매일보** 업로드 →
  SKU × 매장별 주간 판매속도·재고소진주수(WOS) 계산 → 창고→매장 보충 / 매장 간 이관 / 종료매장 회수 추천,
  무판매·저회전, 월간 전체 점검(사장재고·매장 편중), 엑셀 7시트 다운로드.
  이관 유형은 `창고보충`·`재고이동`·`종료매장 회수` 3가지이고 구분·출발·도착으로 필터할 수 있다.
  조회주수는 판매일보 판매일자에서 **주일 단위**로 자동 산출. 반품은 음수로 상계.
  **부자재 제외**: 창고재고현황 `품목=부자재`인 상품코드, 그리고 이름에
  `쇼핑백/지퍼백/선물박스/더스트백/논우븐/원목/스탠드`가 들어가는 포장·집기류는 분석에서 뺀다
  (창고재고가 0이라 창고재고현황에 안 잡히는 부자재를 이름으로 보조 판별).
  `창고보충 최소재고`(기본 20개)보다 창고재고가 적으면 창고보충을 추천하지 않는다 — 매장 간 재고이동에는 적용 안 함.
  **기준값 기본**(매장 발주 주 2~3회 전제) — 조회 4주 · 목표 재고 1주치 · 공급매장 보유 1주치 ·
  최소 이관수량 0개 · 곧 닫는 매장 제외 2주 · 창고보충 최소재고 20개. 목표=보유 같은 값 허용.
  최소 이관수량 0은 '제한 없음'이고, 실제 이동 수량은 최소 1개로 막는다.
  기준값·설명은 `⚙ 기준값` 버튼으로 접어 두고, 표는 `간단히/자세히` 토글로 열 수를 바꾼다
  (기본 간단히 — 이관 추천은 구분·상품·출발·도착·수량·도착소진 6열, 자세히는 16열. `transferSimple`).
  분석을 마치면 `🏬 매장별` 뷰가 먼저 열린다 — 매장 카드(재고·주간판매·소진주수·받을/보낼/부족)를
  누르면 그 매장의 받을 것·보낼 것·부족한 상품 상세가 펼쳐지고, 다시 누르면 접힌다.
  부족 목록에는 못 채운 이유(최소 이관수량 미달 / 창고재고 부족 / 여유 없음)를 적는다 —
  목표를 낮추면 필요수량이 1~2개로 작아져 최소 이관수량에 막히는 게 대부분이다.
  자주 발주하는데 목표를 높게 잡으면 발주로 채워질 몫까지 창고보충으로 쏟아진다(8주치면 176건, 3주치면 23건).
  **매장 상태는 백화점 탭이 정본** — 거기서 기간이 끝나 퇴점된 매장, 백화점 탭에 없는 매장은 종료로 본다.
  PlayMD와 백화점 탭은 표기가 달라(`롯데백화점 잠실점` vs `롯데 잠실점`) `_tfNormStore`로 정규화해 맞춘다.
  칩 클릭은 이 탭에서만 쓰는 예외 지정(`transferStoreOverride`, localStorage).
  **퇴점 매장은 재고·판매 모두 분석에서 제외**한다 — 지난 매장 데이터는 현 시점에 쓸모가 없다.
  잔여재고는 월간 점검과 엑셀 `퇴점매장재고` 시트에 참고로만 남는다.
  매장명에 `행사/특설/이벤트`가 들어가면 **행사 매장**으로 보고 이관에서 뺀다 — 행사용 한정상품이라 주고받지 않는다.
  백화점 탭 종료일까지 남은 기간이 `곧 닫는 매장 제외`(기본 2주)보다 짧으면 **이관·창고보충 대상에서 뺀다** — 보내도 팔 시간이 없다.
  **팝업 매장은 조회기간 중 일부만 영업**하므로 매장 영업주수는 `조회주수 × (그 매장 판매일수 ÷ 전체 판매일수)`로 비례 산출한다(최소 1주).
  등급은 소진주수 기준이되 26주 초과를 **저회전**(주간 1개 미만, 할인·소진 대상)과
  **과잉재고**(주간 1개 이상, 발주 조정 대상)로 나눈다 — 잘 팔리는 상품이 장기재고로 잡히지 않게.
  무판매는 운영 매장에 깔린 것만 주간 목록에 올리고, 창고에만 잠든 재고는 월간 사장재고로 넘긴다.
  자비스에는 SKU별 매장 판매 이력이 없다 — `dailySalesDB`/`deptDB`는 매출액만이고 판매일보 장바구니는 미저장.

## 바로가기
- **로그인(SSO) 주소를 직접 조립하지 말 것.** 코스트코 DSS 바로가기가 Keycloak `openid-connect/auth?...`를
  직접 만들고 `redirect_uri`에 `#/costcokr/dropship/`(이미 프래그먼트가 있는 주소)를 넣어 두었는데,
  `response_mode=fragment`라 로그인 후 `#/costcokr/dropship/&session_state=…&code=…`처럼 프래그먼트가 덧붙어
  SPA 라우터가 경로를 못 찾고 **404**가 났다. 바로가기는 서비스 주소만 두고 로그인은 그쪽에 맡긴다.

## 화면 색
- 배경이 크림색(`#fbf6ec`)이라 옅은 회색 글자는 잘 안 보인다. **힌트·보조 문구는 `#5b6675`**(대비 5.4:1)를 쓴다.
  예전에 쓰던 `#94a3b8`은 대비가 2.4:1이라 읽기 어려워 전부 바꿨다 — 새 문구를 넣을 때도 `#94a3b8`을 쓰지 말 것.
  단 상단 네비·홈 상단 카드처럼 **어두운 배경 위**에서는 밝은 색(`#cfe6e6`·`#a5b4fc` 등)이 맞으므로 건드리지 않는다.

## 배포 방법
```powershell
git add index.html
git commit -m "수정 내용"
git push
```
push하면 GitHub Pages에 자동 반영됨 (1~2분 소요).

## 주의사항
- JS 수정 시 문법 오류에 주의 (오류 시 검색 자체가 안 됨)
- `index.html`이 647KB로 크므로 Read 시 offset/limit 분할 필요
