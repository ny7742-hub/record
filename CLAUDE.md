# 화이트샌즈 판매일보 웹앱

## 프로젝트 개요
화이트샌즈 매장 판매일보 등록용 단일 HTML 웹앱.

- **파일**: `index.html` 단일 파일
- **GitHub**: https://github.com/ny7742-hub/record
- **배포 URL**: https://ny7742-hub.github.io/record/
- **외부 라이브러리**: SheetJS CDN (`xlsx.full.min.js` v0.18.5)

## 현재 기능
- 상품명 + 칼라 통합 검색 (실시간)
- 칼라 칩 자동 표시 및 선택
- 키보드 방향키로 검색결과 탐색, Enter로 추가
- 선택 목록 최신순 정렬, 중복 추가 허용
- 종류별/전체 할인율 일괄 적용
- 판매 양식 엑셀 다운로드 (시트명: 매장판매양식)
- 상품 데이터 업데이트 기능 (xlsx 업로드 → localStorage + Firebase로 팀 전체 공유, 7일 이상 지나면 헤더에 업데이트 필요 경고 배지 표시)
- 매장코드 자동변환: 센텀→LSC, 동부산→LDB, 평촌→LPC, 김포→LKP
- 코스트코 발주: PDF 주문서의 코스트코 상품코드(6자리)를 `CC_PDF_CODE_MAP`으로 자사 상품코드에 연결한 뒤,
  상품데이터에서 자사코드·바코드를 찾는다. **신상품이 코스트코에 등록되면 이 표에 코드를 추가해야 매칭된다**
  — 코스트코 판매현황 상품목록(`costcoDB.products`)에만 넣으면 발주 매칭은 계속 미매칭으로 뜬다.
  상품데이터 조회는 업로드본(`RAW`) → 내장 스냅샷(`DEFAULT_DATA`) 순서(`_ccPdfFindProductRow`).
  바르셀(691716 / `WLW44H5S`)은 **코스트코 단독상품**이라 상품명에 시즌 프리픽스가 없고,
  플레이엠디에 TAG가·현재가가 0으로 등록돼 있어 `CC_ONLY_PRICE`로 25,990원을 채운다(값이 0일 때만).
  코스트코 판매현황의 `바르셀` 정상가도 25,990원으로 강제 보정 — `바르셀 직매입`(20,656원 매입가)은 별개 행이라 그대로 둔다.
  5단계 `⬇ 발송일괄업로드`를 받으면 **"코스트코 탭의 매출을 등록할까요?"** 모달이 뜬다(예/아니오).
  예를 누르면 주문일자를 판매일(M/D)로 삼아 상품·색상별 수량·금액을 코스트코 판매 현황에 **더한다**
  (판매현황 파일 업로드처럼 그 날짜를 통째로 덮어쓰지 않는다).
  2개입 세트상품(`CC_SET_PRODS`)은 업로드 규칙과 동일하게 개수÷2 = 세트 수로 저장한다.
  상품·색상을 못 맞춘 줄은 등록하지 않고 모달에 이유를 적는다.
  **중복 등록 방지는 4중이다** —
  ① 같은 PDF 재업로드: `_ccPdfRowKey`(주문번호|상품코드|컬러코드|원상품명|수량)로 이미 있는 주문 줄은 안 쌓는다.
  ② 같은 주문 재등록: `costcoDB.ccPdfDone`(주문번호|상품|색상#n → 등록시각, 180일 보관)에 남겨 두 번 반영 안 한다.
  Firebase에서 원격 costcoDB로 교체될 때 `discontinued`처럼 **로컬 `ccPdfDone`을 병합**해 이력이 유실되지 않게 한다.
  ③ 판매현황 파일과의 이중계상: 그 날짜에 파일 업로드 실적(`sales[기간]._uploaded`)이 있으면 **기본 제외**하고
  모달에 경고 + 체크박스로만 강제 포함할 수 있다. 그래서 발주 등록은 `_uploaded`를 켜지 않고 `_pdfReg`만 남긴다
  (`_uploaded`는 `ccMergeWeekMap` = 파일 업로드 전용 표시여야 경고 근거가 된다).
  ④ '예' 연타: `_ccPdfSalesBusy` + 계획 즉시 분리.
- 사입출고등록: 홈쇼핑 주문서는 결제금액이 비어 오는 경우가 많아,
  비면 `홈쇼핑 매칭표의 현재가 → 상품데이터(자사코드) 현재가/TAG가` 순으로 단가를 찾아
  `단가 × 수량`으로 채우고 표에 총 수량·총 판매금액을 보여준다(자동 채운 건은 💡 표시, 직접 수정하면 해제).
  주문 상품명 정규화는 **맨 앞의** `(최초공개)`·`(단독)` 같은 괄호 홍보문구를 제거한다
  (이름 중간의 `(Ardes)`는 주문서에서 괄호 없이 오기도 해서 통째로 지우면 오히려 깨진다).
  컬러명은 정확일치 후 **부분일치**로 한 번 더 찾는다 — 주문 `베이지` ↔ 상품데이터 `(핑크&크림)베이지`.
  단 부분일치 후보가 둘 이상이면 비워 둔다.
  컬러를 못 읽었는데 후보 색이 여럿이면 코드를 비워 둔다 — 첫 색을 임의로 고르면 엉뚱한 코드가 나간다.
- 마진계산 → 코스트코 행사 공문 (`💰 마진계산` 하단 `📄 코스트코 행사 공문`):
  엔에이치그룹 공문 **A4 1장**을 PDF로 만든다(html2canvas + jsPDF, 아난티 명세서와 같은 방식).
  **금액은 마진계산 표에서 VAT 제외로 자동 산출** — `쿠폰=(판매가−쿠폰적용가)÷1.1`,
  `Vendor fund=당사부담금÷1.1`, `Costco Fund=코스트코부담÷1.1`. 상품코드가 그대로 공문 내용의 품번이 된다.
  **행사기간은 따로 입력받지 않고 맨 위 `행사기간` 칸(`marginDB.period`)을 파싱해서 쓴다**(`_mgCtPeriod`) —
  `6/8~6/21`·`9/14 ~ 9/27`·`2026.09.14~2026.09.27`·`2026년 9월 14일 ~ …` 형태를 읽고,
  연도를 안 적으면 올해로 보며 종료가 시작보다 앞서면(연말~연초) 종료만 다음 해로 넘긴다.
  수신은 `코스트코 상품본부(D31)`·`(D26)` 중 선택, 판매처는 `코스트코 인라인`·`온라인` 중 선택이고
  **판매처가 공문 제목의 `인라인/온라인 상품`을 정한다**. 문서번호는 발송일자 기준 `NH-MMDD-01` 자동(직접 적으면 우선).
  상품이 여러 건이면 행마다 `공문 표기명`을 적을 수 있고, 비우면 제목의 영문 상품명을 쓴다.
  직인은 이미지 업로드 시 자동 날인되며 `localStorage.mgSealImg`에만 둔다(용량·개인도장이라 팀 공유 안 함).
  KO COUPON CONTRACT(코스트코 쿠폰 계약서) 페이지는 만들지 않는다 — 그 양식은 코스트코 것을 직접 작성한다.
- 아난티 발주 (`🏨 아난티 발주`): 아난티가 보낸 엑셀을 올리면 **바코드**로 자사 상품을 매칭해
  도매발주서·거래명세서로 바꾼다. **양식이 두 가지다** —
  `위탁 거래명세서`는 바코드를 **비고열**에 넣어 보내고(규격·면과세 열 있음),
  `발주 요청 리스트`는 **바코드 전용 열**이 따로 있다(비고는 빔). 전용 열이 있으면 그쪽을 먼저 본다(`cBar`).
  공급율도 양식마다 달라 `공급율` 값이 없으면 `수수료`를 찾아 `공급율 = 1 − 수수료`로 환산한다(둘 다 없으면 0.7).
  문서번호 칸에 위탁 양식은 문서번호(46139)가, 발주 요청 리스트는 날짜가 들어와서 **숫자일 때만** 다음 번호를 제안한다.
  품목명 접두사(`LE_에이치티와이(화이트샌즈)_…`)는 회사명까지만 떼고, 그 뒤 시즌·행사 표기(`C.O`·`26SS`·`50%`)만 추가로 뗀다 —
  무조건 다음 덩어리를 떼면 `페미닌 브로더리…`가 `브로더리…`로, `50%비즈 숏 스트랩`이 `숏 스트랩`으로 깎인다.
  **5단계 거래명세서는 아난티 코브(ANC)만 '위탁 양식'** 이다 — 1단계에 어떤 발주서를 올렸든
  내장 템플릿(`AN_WT_TPL`, base64)으로 발행한다. 품목행 10~49(40건) + 총합계 50행이고,
  남는 행은 기존 삭제 로직이 잘라내며 총합계·SUM 범위·F8/P8 참조가 따라 이동한다.
  템플릿은 **공유수식**(`t="shared"`)을 쓰므로 앱에서 행을 복제하면 안 된다 — 행이 모자라면 템플릿을 다시 넓혀야 한다.
  **마지막 품목행의 굵은 아랫선(`thickBot`)은 행을 잘라낸 뒤 새 마지막 행으로 옮긴다** —
  안 옮기면 잘린 자리(템플릿의 원래 마지막 행)에 선이 남아 표 중간에 줄이 그이고 실제 마지막 줄엔 아랫선이 없어진다.
  ANC 외 매장은 업로드본을 템플릿으로 쓰되, 셀 열은 **파싱한 헤더 위치**(`anMeta.cols`)로 찾는다 —
  예전엔 위탁 양식 열(D/G/H/I/L/S)이 하드코딩돼 있어 발주 요청 리스트를 올리면 값이 엉뚱한 칸에 들어갔다.
  명세서 파일명의 매장 표기는 `AN_INVOICE_NAME`(ANC → `아난티 코브_살롱`).
  **1~3단계를 건너뛰고 4단계부터 시작할 수 있다** — 도매발주서(27열)만 올리면
  `_anRowsFromWholesale`가 상품컬러코드로 상품데이터를 찾아 표를 만들고, 매장(주문번호(쇼핑몰) 열)·
  명세서 일자·공급율(0.7)까지 채운다. 이때 품목명은 아난티 표기가 아니라 **상품데이터 정식 상품명**이 되고,
  명세서 거래처는 `AN_WT_PARTY`(아난티 코브 위탁 거래처) 기본값을 쓴다(`_anInvoiceParty`).
  품목명은 행사·매입 표기(`C.O`·`50%`)와 끝의 사이즈 코드(`_OS`·`_FR`)만 떼고 시즌(`26SS`)·색상(`_WH`·`_BE`)은 남긴다.
  3단계 도매발주서는 **공용 `_wholesaleWorkbook`** 을 쓴다 — `발주서` + `매장주소록` 두 시트에
  파일명은 `화이트샌즈(MMDD) - 도매발주서.xlsx`(붙여 씀). R열 상품코드는 다른 탭과 같이 **8자로 자른다**
  (`GKM116Z9U`처럼 상품코드가 9자인 상품에서만 갈린다).
  상품명은 업로드한 상품데이터(`DB`)가 내장 스냅샷(`DEFAULT_DATA`)보다 우선이다 —
  스냅샷에 `클래스볼`처럼 옛 표기가 남아 있어도 실제 출력은 최신 상품데이터(`글래스볼`)를 따른다. 매장은 상호·파일명 키워드로 추정(코브→ANC, 코드/가평→ANG, 남해→ANT, 빌라쥬→ANV).
  **위탁 발주서는 판매가 열이 비어 오는 경우가 많다** — 그대로 두면 전액 0원짜리 명세서가 나가므로,
  비었거나 0이면 매칭 상품의 **TAG가(없으면 현재가)**로 채우고 💡로 표시한다(`priceAuto`).
  파일에 판매가가 적혀 있으면 그 값을 그대로 쓴다. 매입단가 = 판매가 × 공급율 ÷ 1.1이고 공급율 기본값은 0.7.
- 홈 연매출 (`🛍️ 홈` 상단): `누적`/`월별` 토글로 본다(`homeYtdView`, localStorage).
  `누적`은 채널별(백화점·코스트코·홈쇼핑) 올해 1~현재월 vs 전년 동기,
  `월별`은 월마다 채널별 실적 + 전년 동월 + 전년대비를 보여주고 맨 아래에 누적 행이 붙는다.
  집계는 `_homeYtdByMonth` 한 곳에서만 하고 `_homeYtdThisYear`가 그걸 합산한다 — 두 표가 어긋나지 않게.
  전년엔 실적이 있는데 올해가 0인 달은 ⚠로 표시(실제 0이 아니라 미입력일 수 있음).
- 백화점 탭 매장 기간: 팝업·정규매장 모두 운영기간을 적을 수 있고(`deptStoresMeta[매장].popupPeriod`),
  종료일이 지나면 팝업은 `🏁 팝업종료`, 정규매장은 `🚪 퇴점`으로 표시된다.
  기간 파서는 `2026년3월1일`·`2026-03-01` 같은 연도 표기를 지원하고,
  연도를 안 적었는데 종료가 시작보다 앞서면(연말~연초) 종료만 다음 해로 넘긴다.
- 재고확인 (`📦 재고확인` 드롭다운) — `🏬 백화점몰 재고확인`·`🏭 홈쇼핑몰 재고확인` 두 탭을 묶어 둔다.
- 백화점몰 재고확인: 롯데 파트너오피스 **단품 재고관리 → ExcelExport**
  (`온라인 재고`·`스타일코드`·`옵션` 열)를 올리면 채널상품별 백화점몰 재고를 본다.
  스타일코드는 채널 접미사가 붙어 오므로(`WCFM100L_LT` → `WCFM100L`) `_` 앞만 쓰고,
  옵션 `그레이/free`에서 색상만 떼어 상품데이터와 맞춰 자사코드·바코드를 붙인다
  칼라명 앞 괄호는 색 계열 수식어라 색 이름이 아니다 — `(핑크&크림)베이지`=베이지, `(인디)핑크&베리`=핑크&베리,
  `(X)내츄럴/그레이`=내츄럴/그레이. 괄호를 떼고 `/ & ·`를 지운 **핵심 색명**으로 비교하면
  온라인 표기와 그대로 맞고(`내츄럴그레이`↔`(X)내츄럴/그레이`, `핑크`↔`(인디)핑크&베리`),
  `핑크`가 `(핑크&크림)베이지`에 걸리던 모호함도 없어진다(`_onsColorCore`).
  원문 정확일치 → 핵심 정확일치 → 핵심 부분일치 순이고, 어느 단계든 후보가 둘 이상이면 비운다.
  탭에 들어갈 때 자사코드가 빈 행만 다시 맞춰 본다(`_onsRefreshMatch`) — 파일을 다시 안 올려도 채워지게.
  **보충가능** = 백화점몰 `단품판매상태`가 품절(상태 열이 없으면 재고 0) **이고** 창고재고 > 0.
  바로 채워 넣으면 되는 건이라 요약·필터·정렬 모두 이걸 우선으로 본다.
  두 파일은 **어느 쪽을 먼저 올려도 된다** — 창고재고는 `onsWhPending`(localStorage 전용, 팀 공유 안 함)에
  보관해 뒀다가 백화점몰 파일이 오는 순간 `_onsApplyWh()`로 붙는다. 대조 결과(`rows[].wh`)는 팀에 공유된다.
  화면은 **한 표에 담되 같은 상품명은 `rowspan`으로 셀을 합쳐** 한 덩어리로 보인다(`_onsGroups`).
  열은 `상품명 · 컬러 · 상태 · 창고재고 · 보충여부` 다섯 개뿐 —
  상품코드·자사코드·바코드·현재가·백화점몰 재고수량은 화면에서 빼고 엑셀에만 남긴다(발주에 필요).
  백화점몰 재고수량은 상태 셀 툴팁으로만 보여준다. 표는 `table-layout:fixed`로 열 폭을 고정한다.
  상품 정렬은 보충가능 많은 순 → 품절 많은 순 → 재고 적은 순.
  결과는 팀 공유(`onlineStockDB` / `ws/onlineStock`).
- 재고이관 분석 (`🔄 재고이관` 탭): PlayMD **매장재고현황 + 창고재고현황 + 매장판매일보** 업로드 →
  SKU × 매장별 주간 판매속도·재고소진주수(WOS) 계산 → 창고→매장 보충 / 매장 간 이관 / 종료매장 회수 추천,
  무판매·저회전, 월간 전체 점검(사장재고·매장 편중), 엑셀 7시트 다운로드.
  이관 유형은 `창고보충`·`재고이동`·`종료매장 회수` 3가지이고 구분·출발·도착으로 필터할 수 있다.
  조회주수는 판매일보 판매일자에서 **주일 단위**로 자동 산출. 반품은 음수로 상계.
  **부자재 제외**: 창고재고현황 `품목=부자재`인 상품코드, 그리고 이름에
  `쇼핑백/지퍼백/선물박스/더스트백/논우븐/원목/스탠드`가 들어가는 포장·집기류는 분석에서 뺀다
  (창고재고가 0이라 창고재고현황에 안 잡히는 부자재를 이름으로 보조 판별).
  `창고보충 최소재고`(기본 20개)보다 창고재고가 적으면 창고보충을 추천하지 않는다 — 매장 간 재고이동에는 적용 안 함.
  **기준값 기본**(매장 발주 주 2~3회 전제) — 조회 4주 · 목표 재고 1주치 · 공급매장 보유 1주치 ·
  최소 이관수량 0개 · 곧 닫는 매장 제외 2주 · 창고보충 최소재고 20개. 목표=보유 같은 값 허용.
  최소 이관수량 0은 '제한 없음'이고, 실제 이동 수량은 최소 1개로 막는다.
  기준값·설명은 `⚙ 기준값` 버튼으로 접어 두고, 표는 `간단히/자세히` 토글로 열 수를 바꾼다
  (기본 간단히 — 이관 추천은 구분·상품·출발·도착·수량·도착소진 6열, 자세히는 16열. `transferSimple`).
  분석을 마치면 `🏬 매장별` 뷰가 먼저 열린다 — 매장 카드(재고·주간판매·소진주수·받을/보낼/부족)를
  누르면 그 매장의 받을 것·보낼 것·부족한 상품 상세가 펼쳐지고, 다시 누르면 접힌다.
  부족 목록에는 못 채운 이유(최소 이관수량 미달 / 창고재고 부족 / 여유 없음)를 적는다 —
  목표를 낮추면 필요수량이 1~2개로 작아져 최소 이관수량에 막히는 게 대부분이다.
  자주 발주하는데 목표를 높게 잡으면 발주로 채워질 몫까지 창고보충으로 쏟아진다(8주치면 176건, 3주치면 23건).
  **매장 상태는 백화점 탭이 정본** — 거기서 기간이 끝나 퇴점된 매장, 백화점 탭에 없는 매장은 종료로 본다.
  PlayMD와 백화점 탭은 표기가 달라(`롯데백화점 잠실점` vs `롯데 잠실점`) `_tfNormStore`로 정규화해 맞춘다.
  칩 클릭은 이 탭에서만 쓰는 예외 지정(`transferStoreOverride`, localStorage).
  **퇴점 매장은 재고·판매 모두 분석에서 제외**한다 — 지난 매장 데이터는 현 시점에 쓸모가 없다.
  잔여재고는 월간 점검과 엑셀 `퇴점매장재고` 시트에 참고로만 남는다.
  매장명에 `행사/특설/이벤트`가 들어가면 **행사 매장**으로 보고 이관에서 뺀다 — 행사용 한정상품이라 주고받지 않는다.
  백화점 탭 종료일까지 남은 기간이 `곧 닫는 매장 제외`(기본 2주)보다 짧으면 **이관·창고보충 대상에서 뺀다** — 보내도 팔 시간이 없다.
  **팝업 매장은 조회기간 중 일부만 영업**하므로 매장 영업주수는 `조회주수 × (그 매장 판매일수 ÷ 전체 판매일수)`로 비례 산출한다(최소 1주).
  등급은 소진주수 기준이되 26주 초과를 **저회전**(주간 1개 미만, 할인·소진 대상)과
  **과잉재고**(주간 1개 이상, 발주 조정 대상)로 나눈다 — 잘 팔리는 상품이 장기재고로 잡히지 않게.
  무판매는 운영 매장에 깔린 것만 주간 목록에 올리고, 창고에만 잠든 재고는 월간 사장재고로 넘긴다.
  자비스에는 SKU별 매장 판매 이력이 없다 — `dailySalesDB`/`deptDB`는 매출액만이고 판매일보 장바구니는 미저장.

## 배포 방법
```powershell
git add index.html
git commit -m "수정 내용"
git push
```
push하면 GitHub Pages에 자동 반영됨 (1~2분 소요).

## 주의사항
- JS 수정 시 문법 오류에 주의 (오류 시 검색 자체가 안 됨)
- `index.html`이 647KB로 크므로 Read 시 offset/limit 분할 필요
